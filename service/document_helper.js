const multer = require('multer');
const cosmo_surfer = require('./cosmo_util');

module.exports = {
    // A function to process files retrieved from the request body
    async processFiles(file, context, chatId, messages, database, container, bot) {


    }
}